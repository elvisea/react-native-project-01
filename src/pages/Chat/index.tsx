import React from 'react';

import {Container, TitlePage} from './styles';

const Chat: React.FC = () => {
  return (
    <Container>
      <TitlePage>Chat</TitlePage>
    </Container>
  );
};

export default Chat;
